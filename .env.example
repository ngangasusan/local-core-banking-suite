# URL of the Express/MySQL backend (backend/ folder).
# Must NOT be the same origin as the Vite dev server, or every API call 404s.
# Example: run the backend with PORT=4000 and set:
VITE_API_URL=http://localhost:4000
